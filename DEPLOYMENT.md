# GitHub Pages 自动化部署完整指南

本文档详细说明如何配置 GitHub Actions 实现博客的自动化部署。

---

## 📋 前提条件

- ✅ GitHub 账号
- ✅ 本地已完成 Vue 2.7 升级
- ✅ 项目可以本地成功构建（`pnpm build`）

---

## 🚀 第一步：检查并配置项目

### 1.1 确认主分支名称

```bash
# 查看当前分支
git branch

# 如果显示 * master，则主分支是 master
# 如果显示 * main，则主分支是 main
```

### 1.2 修改 workflow 配置（如果需要）

如果您的主分支是 `master`，需要修改 `.github/workflows/deploy.yml`：

```yaml
on:
  push:
    branches:
      - master # 改为 master
```

### 1.3 确认自定义域名配置

**如果您有自定义域名** `guqzhou.eu.org`，保持 `deploy.yml` 中的配置：

```yaml
cname: guqzhou.eu.org
```

**如果没有自定义域名**，删除这一行：

```yaml
# 删除下面这行
cname: guqzhou.eu.org
```

---

## 🔧 第二步：本地测试构建

在推送到 GitHub 之前，先确保本地构建成功：

```bash
# 1. 清理旧的构建产物
rm -rf docs/.vuepress/dist

# 2. 执行构建
pnpm build

# 3. 检查构建结果
ls docs/.vuepress/dist
# 应该看到 index.html 和其他静态文件
```

如果构建失败，解决问题后再继续。

---

## 📤 第三步：推送代码到 GitHub

### 3.1 添加并提交更改

```bash
# 1. 查看修改的文件
git status

# 2. 添加所有更改
git add .

# 3. 提交更改
git commit -m "feat: 升级到 Vue 2.7 并添加 GitHub Actions 自动部署"

# 4. 推送到 GitHub
git push origin main  # 或 master
```

### 3.2 查看 Actions 运行状态

1. 打开浏览器，访问：

   ```
   https://github.com/guqzhou/guqzhou.github.io/actions
   ```

2. 您会看到一个名为 "Deploy VuePress Blog" 的 workflow 正在运行

3. 点击进入查看详细日志

4. 等待构建完成（首次约 5-10 分钟，后续 2-3 分钟）

---

## ⚙️ 第四步：配置 GitHub Pages

### 4.1 进入仓库设置

1. 访问：`https://github.com/guqzhou/guqzhou.github.io/settings/pages`
2. 或者：仓库页面 → Settings → 左侧菜单 Pages

### 4.2 配置 Pages 设置

在 "Build and deployment" 部分：

- **Source**: 选择 `Deploy from a branch`
- **Branch**: 选择 `gh-pages` 分支，目录选择 `/ (root)`
- 点击 **Save** 按钮

![GitHub Pages 设置示例](https://docs.github.com/assets/cb-47267/mw-1440/images/help/pages/publishing-source-drop-down.webp)

### 4.3 配置自定义域名（可选）

**如果您有自定义域名**：

1. 在 "Custom domain" 输入框中填入：`guqzhou.eu.org`
2. 点击 **Save**
3. 等待 DNS 检查完成
4. 勾选 "Enforce HTTPS"（推荐）

**域名 DNS 配置**：
在您的域名提供商处添加以下记录：

```
类型: CNAME
名称: @（或留空）
值: guqzhou.github.io
```

---

## ✅ 第五步：验证部署

### 5.1 等待部署完成

1. 返回 Actions 页面
2. 确认 workflow 显示绿色 ✓（成功）
3. 等待 1-2 分钟让 GitHub Pages 更新

### 5.2 访问您的博客

- **有自定义域名**: https://guqzhou.eu.org
- **无自定义域名**: https://guqzhou.github.io

### 5.3 检查页面

确认以下内容正常：

- ✅ 首页正常显示
- ✅ 导航菜单可以点击
- ✅ 文章列表正常
- ✅ 文章内容可以查看
- ✅ 搜索功能正常

---

## 🔄 日常使用流程

配置完成后，每次更新博客只需：

```bash
# 1. 编写或修改文章
# 2. 本地预览（可选）
pnpm dev

# 3. 提交并推送
git add .
git commit -m "docs: 添加新文章"
git push origin main

# 4. 等待自动部署（2-3 分钟）
# 5. 访问博客查看更新
```

---

## 🐛 常见问题排查

### 问题 1: Actions 构建失败

**查看日志**：

1. 进入 Actions 页面
2. 点击失败的 workflow
3. 查看红色 ❌ 的步骤
4. 展开查看详细错误信息

**常见错误**：

- **依赖安装失败**: 检查 `package.json` 和 `pnpm-lock.yaml`
- **构建失败**: 本地运行 `pnpm build` 复现问题
- **权限错误**: 检查 GitHub token 权限

### 问题 2: 部署成功但页面空白

**可能原因**：

1. `base` 配置错误

   检查 `docs/.vuepress/config.js`：

   ```javascript
   module.exports = {
     base: "/", // 应该是 '/'
   };
   ```

2. 静态资源路径错误

   确认 `public` 目录下的资源路径正确

### 问题 3: 自定义域名无法访问

**检查步骤**：

1. DNS 记录是否正确配置
2. DNS 是否已生效（可能需要 24 小时）
3. GitHub Pages 设置中域名是否正确
4. 是否启用了 HTTPS

**验证 DNS**：

```bash
# macOS/Linux
nslookup guqzhou.eu.org

# 应该看到指向 guqzhou.github.io 的记录
```

### 问题 4: 更新后页面没有变化

**解决方法**：

1. 清除浏览器缓存（Cmd+Shift+R 或 Ctrl+Shift+R）
2. 等待 2-3 分钟（CDN 缓存更新）
3. 检查 Actions 是否成功运行
4. 确认推送的是正确的分支

---

## 🎯 手动触发部署

如果需要手动触发部署（不推送代码）：

1. 访问：`https://github.com/guqzhou/guqzhou.github.io/actions`
2. 点击左侧 "Deploy VuePress Blog"
3. 点击右侧 "Run workflow" 按钮
4. 选择分支（main 或 master）
5. 点击绿色 "Run workflow" 按钮

---

## 📊 部署状态徽章（可选）

在 `README.md` 中添加部署状态徽章：

```markdown
[![Deploy](https://github.com/guqzhou/guqzhou.github.io/actions/workflows/deploy.yml/badge.svg)](https://github.com/guqzhou/guqzhou.github.io/actions/workflows/deploy.yml)
```

效果：![Deploy](https://github.com/guqzhou/guqzhou.github.io/actions/workflows/deploy.yml/badge.svg)

---

## 🔐 安全建议

1. **不要提交敏感信息**：

   - API 密钥
   - 密码
   - 个人令牌

2. **使用 GitHub Secrets**（如需要）：

   - Settings → Secrets and variables → Actions
   - 添加敏感配置

3. **定期更新依赖**：
   ```bash
   pnpm update
   ```

---

## 📚 相关资源

- [GitHub Actions 文档](https://docs.github.com/cn/actions)
- [GitHub Pages 文档](https://docs.github.com/cn/pages)
- [VuePress 文档](https://vuepress.vuejs.org/)
- [pnpm 文档](https://pnpm.io/)

---

## ✨ 完成！

恭喜！您的博客现在已经配置了自动化部署。每次推送代码，GitHub Actions 会自动构建并部署您的博客。

**享受写作吧！** 🎉
